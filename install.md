# Instructions

1. Link in config files:
```bash
ln -s ~/.config/alacritty alacritty
ln -s ~/.config/nvim nvim
ln -s ~/.tmux.conf tmux.conf
```

4. install nerd fonts (depends on OS)
5. resolve nvim dependencies
6. profit
